
search をLucene Query専用にしてよいと思います。このとき、古い関数は削除してもよいと思います。

---

はい。その方針なら、APIを一度整理してしまう価値があります。特に **`search()` を「Lucene Query Syntaxによる検索」として正式APIに統一し、`search_lucene()` は移行後に削除する**のが分かりやすいです。

現状の `SearchEngine.search()` は、文字列なら通常全文検索、`list/tuple` ならベクトル検索、さらに `filters=` も受ける多目的APIになっています。 一方、今回確認した `example_016_lucenequery.py` では `search_lucene()` により Lucene構文の AND / OR / NOT / field指定 / wildcard / phrase がすでに自然に使えています。

私は次のように整理するのを推します。

## 新しい基本API

```python
results = engine.search(
    "category:company AND text_en:Kyoto",
    limit=10,
)
```

つまり、

```python
search(query: str, limit: int = 10)
```

は **Lucene Query専用**です。

CLIも完全に一致します。

```text
>> search "category:company AND text_en:Kyoto"
```

↓

```python
engine.search(
    "category:company AND text_en:Kyoto",
    limit=10,
)
```

これならユーザーがPythonとCLIで別の検索言語を覚える必要がありません。

---

## `search_lucene()` は最終的に削除してよい

現在、

```python
engine.search_lucene("category:city", 10)
```

としているものを、

```python
engine.search("category:city", 10)
```

へ統一します。

したがって、

```text
search_lucene()
    ↓
deprecated期間を置く、または0.xなら直接削除
    ↓
search()
```

です。

まだ `0.x.x` 系でAPIを固めている段階なら、互換性を無理に維持するより、**今ここで削除してAPIを単純化する**のは合理的だと思います。

---

## 現在の通常全文検索 `search("Kyoto")` はどうするか

Lucene QueryParserでは、

```python
engine.search("Kyoto")
```

自体も検索式として成立させることができます。

ここでデフォルトフィールドが `text_en` / `text_ja` に設定されるJava実装なら、

```python
engine.search("Kyoto")
```

と、

```python
engine.search("text_en:Kyoto")
```

の両方を使える設計にできます。

その場合、従来の「通常全文検索」を別APIとして残す必要性はかなり低くなります。

理想的には、

```python
engine.search("Kyoto")
engine.search("Kyoto AND historic")
engine.search("category:city")
engine.search("category:city AND Kyoto")
engine.search('text_en:"historic city"')
```

を全部 `search()` で処理します。

これはかなり使いやすいです。

---

## `filters=` も削除候補

現行APIでは、

```python
engine.search(
    "Kyoto",
    filters={"category": "city"},
)
```

という書き方があります。

Lucene Queryへ統一するなら、

```python
engine.search(
    "text_en:Kyoto AND category:city"
)
```

で表現できます。

したがって高レベルAPIの `filters=` も削除してよいと思います。

現在の、

```python
search(
    query,
    limit=10,
    *,
    filters=None,
)
```

から、

```python
search(
    query: str,
    limit: int = 10,
) -> list[SearchResult]:
```

まで単純化できます。

かなりすっきりします。

---

## ただしベクトル検索だけは分離する

ここは重要です。

現在の `search()` は、

```python
engine.search([0.9, 0.1], limit=10)
```

というベクトル検索まで兼ねています。

Lucene Query専用へ整理するなら、これは別メソッドにした方がよいです。

例えば、

```python
engine.search_vector(
    vector,
    limit=10,
)
```

です。

すると責務が明確になります。

```python
# Lucene query
engine.search(
    "category:company AND text_en:Kyoto"
)

# Vector search
engine.search_vector(
    [0.12, 0.43, ...]
)
```

EmbeddingProviderを使うなら将来的には、

```python
engine.search_semantic(
    "historic Japanese city"
)
```

のような高レベルAPIも検討できます。

ただ、`search()` の中で `str` と `list[float]` を判定する現在の方式は、今回の整理を機にやめてもよいと思います。

---

## OpenSearch DSL APIは残す

一方、

```python
search_json()
search_response_json()
```

は削除しなくてよいでしょう。

これはLucene Query Syntaxとは目的が異なります。

例えば、

```python
engine.search({
    ...
})
```

のように全部 `search()` に押し込むより、

```python
engine.search("category:city")       # Lucene Query
engine.search_json({...})            # OpenSearch-style DSL
engine.search_vector([...])          # Vector
```

と明示的に分ける方が分かりやすいです。

最終的には、

| API                          | 用途                      |
| ---------------------------- | ----------------------- |
| `search(str)`                | Lucene Query Syntax     |
| `search_json(dict)`          | OpenSearch Query DSL    |
| `search_response_json(dict)` | OpenSearch形式の完全response |
| `search_vector(vector)`      | ベクトル検索                  |

という形です。

---

## `aggregate()` / `view()` との整合性もよくなる

現在 `aggregate()` には、

```python
query=
lucene_query=
filters=
```

という3種類の検索条件があります。

検索APIをLucene Query中心に整理するなら、こちらも将来的には、

```python
engine.aggregate(
    "category",
    query="text_en:Kyoto AND country:Japan",
)
```

に統一できます。

現在の、

```python
engine.view(
    "part",
    "maker:Nissan",
)
```

はすでにLucene Queryを採用しているので、 むしろ `search()` 側をこれに合わせる形になります。

つまりAPI全体が、

```python
engine.search("maker:Nissan AND text_en:brake")

engine.view(
    "part",
    "maker:Nissan AND text_en:brake",
)

engine.aggregate(
    "part",
    query="maker:Nissan AND text_en:brake",
)
```

という一貫した形にできます。

これはかなり大きな改善です。

---

## `example_016` も正式な `search()` サンプルへ変更

現在は、

```python
engine.search_lucene("category:city", 10)
```

ですが、

今後は、

```python
engine.search("category:city", 10)

engine.search(
    "category:company AND text_en:Kyoto",
    10,
)

engine.search(
    'text_en:"historic city"',
    10,
)
```

とします。

サンプル名も、

```text
example_016_lucenequery.py
```

のままで問題ありません。

むしろ、

> `SearchEngine.search()` accepts Lucene Query Syntax.

という重要なサンプルになります。

---

## CLIとの対応も完全に1:1になります

最終的に、

```text
Python API                         CLI
────────────────────────────────────────────────────
engine.data("x.jsonl").load()     load x.jsonl

engine.search("category:city")    search "category:city"

engine.fields()                   fields

engine.view()                     view

engine.view("year")               view year

engine.view("year",
 "category:company")              view year "category:company"
```

になります。

これは非常に良い状態です。

### 推奨する変更順

1. 現在の `search_lucene()` の中身を `search()` に移す
2. `search()` から通常検索・vector overload・`filters=` を削除
3. vector検索を `search_vector()` に移す
4. `search_lucene()` を削除
5. `example_016` を `search()` に変更
6. 既存example/testの `search()` 呼び出しをLucene Query形式へ変更
7. `aggregate()` の `query/lucene_query/filters` は別変更として後で整理
8. その上にCLIの `search` を実装

特に **CLIを作る前にPython側の `search()` の意味を固定する**のがよいです。

`search()` = Lucene Query、`view()` = Lucene Queryを使った分析、という二本柱になることで、

```text
load → search → view
```

というCLIのコンセプトが非常に単純になります。0.x系の今なら、古い多目的 `search()` と `search_lucene()` を両方抱え続けるより、このタイミングで整理してしまう方が長期的には保守しやすいと思います。


